# 全量弹窗与浮层复刻｜强制验收补充契约

## 用户要求与当前状态
用户明确要求：包括弹窗，全部需要。本契约补充 AGENTS.md 和 COVERAGE_AUDIT.md，不替代已有页面补漏任务。
弹窗、浮层、瞬时提示及其交互均为正式交付项，不能只交主页面、打开状态截图或一个通用弹窗模板。
当前仅完成范围与验收规则落地；全量弹层盘点、实现、运行和视觉验收尚未完成。overlay_scope_closed=false，total_verified_overlays=null。独立 LLM 子代理启动数仍为 0。

## 待逐模块核对的类别
以下是排查类别，不是已确认全部存在于销帮的组件清单。原源码、可复核运行证据或授权参考确认存在后登记；未发现或无法触发时保留待核实项，不能自行补造。

| 类别 | 排查范围 |
|---|---|
| 居中对话框 | 确认、取消、删除、释放、提交、保存、放弃修改、提醒和业务说明 |
| 底部弹层和抽屉 | 操作菜单、更多操作、分享、认领/分配、筛选、排序、日期/时间/范围和多级选择 |
| 锚点浮层 | 下拉菜单、气泡提示、图表提示、长按菜单、上下文菜单和引导浮层 |
| 输入与选择交互 | 弹窗表单、搜索选择、人员/部门/行业/地区/门店选择、二次确认和嵌套弹层 |
| 瞬时状态 | Toast、加载遮罩、提交中、成功/失败/警告、重试、禁用、校验提示 |
| 原生与第三方界面 | 应用触发的权限请求、相册/相机/文件选择、系统分享、外部应用跳转确认、系统返回与键盘联动 |

Android 系统或第三方提供的界面单独记录提供方、系统版本与触发流程；本地模拟不得标为真实系统截图。仅将 App 实际调用的功能纳入，不推定每类均存在。

## 独立登记与证据
在 inventory/overlay-registry.json 独立登记，每个业务入口/模式/角色不同的弹层变体分别记录；可以复用视觉组件，但不能据此跳过触发路径和状态验证。
每项至少包括：id、所属应用/页面 ID、完整路由、源版本/文件哈希/代码位置、组件、触发动作、角色/灰度/前置数据、弹层层级、父子弹层关系、初始状态、确实存在的加载/有数据/空/错误/选中/禁用/校验状态、确认/取消结果、关闭策略、截图/测试/设计节点、差异与阻塞。
关闭策略逐项核对：关闭按钮、取消、点击遮罩、Android 返回、下滑手势、键盘收起、自动消失、跳转离开。原版不支持的方式必须明确记为不允许或不适用，并附依据。
同一页面可关联多个弹层；弹层可关联多个页面。redirect/container 等记录排除时要有可复核理由。瞬时提示不计为独立业务页面；资源或引用次数不计为弹窗数。

## 发现方法与防漏要求
SA01 在现有路由盘点之外，加查组件引用和调用点、动态导入、别名/枚举、Portal/弹层宿主、visible/open 状态控制、自定义 CSS 遮罩、原生 Dialog/PopupWindow 和系统桥接调用。
上述关键词只是线索；必须结合调用上下文和实际触发交叉对账。框架内部实现、未使用导出、重复引用逐项说明排除理由；不能仅凭搜索命中宣称完成。
每个已确认页面走查点击、长按、返回、更多菜单、筛选、提交、失败与无权限分支。无法访问的配置、动态 schema、未采集懒加载和缺少参考保留为阻塞项，不绕过原服务认证。

## 分工与补漏任务
| 任务 | 责任 | 输出 |
|---|---|---|
| COV-07 | SA01 牵头，SA02–SA06 提交模块证据 | 全量弹层及触发点登记、来源差集与排除依据 |
| COV-08 | SA02–SA06 实现，SA07 输出可编辑组件与变体 | 离线可触发弹层、数据/错误状态、嵌套选择、确认与关闭回路 |
| COV-09 | SA08 独立验收，主控合并 | 遮罩/返回/键盘/取消副作用/确认结果/重复触发/叠层及视觉对比证据 |

所有 workers/SA01…SA08/TASK.md 均须包含本契约。只有主控合并共享登记表。任务分派不能当作真实子代理启动证明。

## 验收标准
视觉按同版本、同角色、同设备视口/密度/字号设置核对尺寸、定位、圆角、阴影、遮罩透明度、文字、图标、内边距、层级、安全区及键盘避让。动画按原证据核对方向、时长、曲线和打断行为；缺少参考即未验证。
行为必须覆盖：从真实入口打开 → 更改/选择 → 确认或取消 → 返回所属页面并核对结果；嵌套弹层关闭只影响正确层级；取消不产生不应有的数据或业务副作用；确认只操作本地 fixture，禁止发送生产请求。
每项保留 source_status、render_status、interaction_status、visual_status、design_status，状态之间不得互相替代。关键中间态用可复现截图或录屏取证，瞬时提示不通过人为延长时长冒充原交互。
没有弹层登记、触发证据或明确排除依据的页面不得标为完整。页面全量门禁和弹层全量门禁分开；任一范围未知、阻塞未解决、视觉未验证、可编辑设计未交付时，G0 保持 in_progress。

## 本轮变更边界
本轮更新执行契约、任务登记和门禁检查；不宣称新增弹窗已经实现、原缺漏已经修复、所有页面无遗漏，或有子代理在后台运行。
