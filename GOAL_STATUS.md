# 全量 UI 复刻｜G0 进度检查点

## 总目标
所有界面、关键状态、弹层和跳转流程均纳入；以完整离线页面、可编辑设计源文件、组件库、截图和测试为交付。G0 状态为 in_progress，不能以几个演示页结项。

## 分工与真实执行状态
AGENTS.md 已定义 SA01–SA08 八个独立任务契约，分别负责全量盘点、客户线索、行业分支、首页工具拜访日报、数据业绩、消息原生导航、设计系统、独立 QA。
独立 LLM 子代理启动数：0。当前聊天没有子代理启动接口。本轮实际使用了公开静态资源并行采集、AST/原生清单脚本、3 个并行浏览器 worker 和主控整合。以下结果均不代表 LLM 子代理已启动。

## 本轮可复核结果
| 项目 | 结果 | 边界 |
|---|---|---|
| 微应用入口 | 13/13 已归档；新增 158 个静态资源文件 | 各 H5 版本分别锁定，与 APK 2.4.5 独立 |
| 跨应用源码扫描 | 213 条 AST 路由候选，解析错误 0 | 候选未全部确认为真实可达页面 |
| CRM 路由解析 | 87 条路径：68 页面路由、13 容器、5 重定向、1 兜底 | 多个角色复用组件，不能等同 87 个独立界面 |
| 原组件离线试运行 | 24 个不同页面组件 | 15 个可浏览状态；9 个运行或 fixture 缺口 |
| 原生声明 | 120 条 Activity/alias，其中应用命名空间 39 条 | 含 SDK，不等于业务页面数 |
| 旧版回归 | 7 项检查重新通过 | 仅既有客户空态及排序筛选范围 |
| 本轮交互与尺寸 | 9/9 检查通过 | 任务切换、类型选择、空表单保护、搜索、字数、审核标签、机会标签、360/430 宽度 |
| 工作台检查 | 5/5 通过 | 隔离 iframe、切换、缺口展示和手机宽度 |
| 真机像素验收 / 可编辑 Figma | 尚未完成 | 不填报 1:1 完成率 |

以上口径有交叉，不相加为全 App 总页数。全量业务页面总数仍未知，scope_closed=false。

## 已修复和仍有的问题
创建客户原组件依赖宿主 #app。离线宿主补该元素后可显示表单，原渲染函数未重画。记录为宿主兼容调整，不据此声称与真机一致。
浏览测试首轮有隐藏弹层/固定页脚的定位歧义，已保存失败记录并修正定位后重跑通过。
7 个取回失败的 JS URL 对应 webpack 加载器明确跳过的 CSS-only ID；保留原始请求记录，不计为业务页面缺失。

仍需处理：客户/线索/开发详情的参数及本地数据合约；POI 选择的表单上下文；门店详情/创建结果的 undefined 与无效日期；地图 SDK 离线依赖。其他行业、数据、首页、拜访、日报、消息微应用尚未全量渲染。all-in-one 动态页面目录未闭合。

所有预览使用本地 fixture；业务接口请求未发送。原字体、原生导航、授权角色与同版本真机参考仍待验证。最终可编辑设计源文件尚未交付。

## 可执行交接包
本轮对话附件 xiaobang_all_ui_workpack.zip 包含：
- AGENTS.md 和 workers/SA01…SA08/TASK.md：目标、输入输出、写入边界、验收和阻塞处理。
- coordination/tasks.json、execution-receipt.json、KNOWN_BLOCKERS.md：实际状态和执行记录。
- inventory/screen-registry.json：395 条声明/路由证据加 1 条动态微应用入口缺口，共 396 条登记记录，不是页面数。
- index.html：全量目录与阶段原组件预览工作台。
- 原始 source/、source-supplement/、离线适配脚本、截图、QA 和 SHA256 清单；不含字体文件。

当前仓库保存协调规则与采集工作流。完整工作文件在本轮交付包中；启动后续执行环境时先载入工作包，不能假定全部源码已提交到仓库。

## 执行证据
目标契约提交：b6bd761ad3385a1bdbf6eaeededc72a8050c7272。
来源采集提交：e547327af051e220b7eda336a5f2f2adee53ab7f。
GitHub Actions run：35375904654，结论 success；artifact：10560675859。
本轮采集与测试已结束；没有独立 LLM 子代理在后台持续运行。
