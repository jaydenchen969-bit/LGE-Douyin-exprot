# 抖音销帮全量 UI 复刻｜主控与子代理执行契约

## 总目标 G0
用户要求：所有界面，1:1 完整复刻；不止登录页、首页或少数演示页面。
目标交付：完整页面目录、可运行的离线页面与关键状态、可编辑设计源文件/组件库、逐页截图和可复现测试、视觉差异与未解决项记录。

## 来源基准
APK：com.bytedance.lifeservice.crm / 2.4.5 / versionCode 20405。
H5：以已归档 micro-apps.json 中的 13 个发布版本分别锁定，与 APK 版本独立记录。不得声称当前 H5 是 APK 发布时的完整在线界面。
静态来源、原组件离线渲染、授权真机参考、视觉验收分开记录。发现未知页面必须加入清单，不能删除缺口以达成完成率。

## 执行真实性
当前聊天没有独立 LLM 子代理启动工具。下列为可交接的角色和任务契约；并行脚本和 GitHub Actions 作业不等于 LLM 子代理。
真实子代理只有拿到启动工具返回的运行 ID 后才能标为 launched。否则记录 not_launched，并记录实际执行方式（主控、并行脚本或 CI）。
不得声称后台持续工作或在没有工具结果时报告完成。

## 分工
| ID | 范围 | 独占输出目录 |
|---|---|---|
| SA01 | 全量来源、原生 Activity、13 个微应用、路由和状态清单 | workers/SA01/ |
| SA02 | 客户、线索、详情、搜索、新建、认领与释放界面 | workers/SA02/ |
| SA03 | 餐饮、综合、酒旅、自助、区域及代码声明的行业分支 | workers/SA03/ |
| SA04 | 首页、工具、拜访、任务、日报、电子名片 | workers/SA04/ |
| SA05 | 数据中心、业绩、经营分、指标下钻、筛选及低代码依赖 | workers/SA05/ |
| SA06 | 消息、个人/设置、原生登录导航、系统桥接 | workers/SA06/ |
| SA07 | 设计 Token、组件库、可编辑 Figma/设计源文件 | workers/SA07/ |
| SA08 | 独立视觉 QA、交互、比例、网络隔离和覆盖验收 | workers/SA08/ |

只有主控合并 screen-registry.json、tasks.json、最终工作台入口与交付包。任务使用独立目录或 worktree，不并行修改共享文件；需要共享修改时提交 proposal.patch。

## 波次
A：SA01 盘点全部来源；SA02 扩展客户模块；SA08 重跑既有基线。
B：SA03/SA04/SA05/SA06 在来源核实后并行；SA01 持续补目录。
C：SA07 消费已渲染已审查页面；SA08 对每批做回归，主控统一合并。

## 每页验收
逐页记录 source_status、render_status、interaction_status、visual_status、design_status。
source-ready 不等于 rendered，不等于 interaction-verified，不等于 pixel-verified，不等于 delivered。
完成需具备：可追溯来源；正常/空/加载/错误及确实存在的禁用/选中/弹层状态清单；原比例截图；交互断言；无未处理错误；无生产服务请求；文字/布局可编辑；同版本同角色参考对比通过。
缺少授权运行截图、角色配置或低代码 schema 时必须标为 blocked/unverified；不得靠猜测补原界面。重定向、布局容器、资源数和 XML 数不能当作独立业务页面或已完成页。

## 禁止事项
只读取公开静态文件与已有 APK，所有业务响应仅使用本地演示 fixture。不得获取 token、尝试登录、访问业务 API、伪造原服务权限或读取真实客户数据。
source/ 保留原始文件和哈希，对副本的插桩/上下文替换逐项登记。浏览器隔离外部请求，并使用 CSP connect-src none。
不附带字体文件；字体替代列为待校准差异。不用原资源名猜运行时 Tab。
不把页面做成整张 PNG 冒充可编辑设计。Figma 多团队未明确时不得随机选团队创建。

## 交接
每项输出任务 ID、实际运行方式和工具返回 ID、源版本与哈希、变更文件、页面/状态列表、测试命令和结果、截图路径、阻塞项、后续最小步骤。
用户确认目标不等于确认完成；在全量验收通过前保留 G0 为 in_progress。

## 强制补充：全部弹窗、浮层与瞬时状态
用户补充要求：包括弹窗，全部需要。执行前必须同时读取 OVERLAY_ACCEPTANCE.md 与 COVERAGE_AUDIT.md。
SA01 独立盘点 inventory/overlay-registry.json；SA02–SA06 对所属页面的每一处触发入口、角色、模式、嵌套层级及确认/取消回路负责；SA07 交付可编辑组件与变体；SA08 单独做弹层验收。任务 COV-07、COV-08、COV-09 纳入主控，已有 COV-01–COV-06 不得关闭或删除。
中心对话框、底部弹层、抽屉、筛选/排序/日期/多级选择、锚点浮层、Toast/加载/校验/错误反馈，以及应用实际触发的系统和第三方弹窗，均属于排查范围；是否存在必须取证，不能凭类别补造。
同一弹窗组件的不同入口或模式分别验证。只有路由扫描、组件引用或一张打开截图均不能证明弹窗齐全。
页面必须记录 overlay_census_status、overlay_ids、overlay_exclusion_evidence；尚未盘点时使用 not-enumerated，空列表不代表没有弹窗。没有独立路由的弹层同样登记。
验收覆盖打开、更改、二级弹层、确认、取消、遮罩、返回、手势、键盘、安全区、动效及关闭后的结果；不适用项附原版依据。业务操作仅限本地 fixture。
页面与弹层分设完整性门禁。overlay_scope_closed=false 或弹层未知/缺证据/未验收时，不得把整页或 G0 标为完成。不得以新增任务契约冒充已经启动子代理或完成弹窗实现。
